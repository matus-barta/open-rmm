/**
 * Default route for redirects after auth events and some other uses.
 */
export const defaultRoute = '/dashboard/rmm';

/**
 * Disables registration on webpage.
 *
 * If using this setting make sure you have disabled registration in Supabase
 * @link https://supabase.com/docs/guides/auth/auth-user-management#configuration
 */
export const registrationEnabled = true;
