{
  "name": "open-rmm",
  "version": "0.0.1",
  "private": false,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "preinstall": "npx only-allow pnpm",
    "server:dev": "turbo run dev --filter=!www"
  },
  "devDependencies": {
    "prettier": "^2.8.6",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {},
  "packageManager": "pnpm@7.15.0"
}
