export async function createAntivirus(/*input*/) {
	try {
		//return await AntivirusModel.create(input);
		// eslint-disable-next-line @typescript-eslint/no-explicit-any
	} catch (e: any) {
		throw new Error(e);
	}
}
